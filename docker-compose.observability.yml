# =============================================================================
# Observabilidade - Duas pilhas disponíveis (execute UMA por vez)
# =============================================================================
# PILHA LGTM (padrão): docker-compose -f docker-compose.observability.yml up -d
# PILHA SigNoz:        docker-compose -f docker-compose.observability-signoz.yml up -d
#
# Ambas usam OTEL nas portas 4317/4318 - não execute as duas simultaneamente.
# =============================================================================

include:
  - docker-compose.observability-lgtm.yml
